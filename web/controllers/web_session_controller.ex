defmodule Portal.WebSessionController do
    use Portal.Web, :controller

    def new(conn, _params) do
        render(conn, "new.html", [])
    end

    def create(conn, args) do
        
    end

    def delete(conn, args) do
        
    end
end